from tf import transformations
import math

angle = float(raw_input("Enter robot angle in degrees: "))

quat = transformations.quaternion_from_euler(0, 0, math.radians(angle))

print float(quat[0])
print float(quat[1])
print float(quat[2])
print float(quat[3])




